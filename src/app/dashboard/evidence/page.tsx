import { ModulePlaceholder } from "@/components/ModulePlaceholder";

export default function EvidencePage() {
  return (
    <ModulePlaceholder
      title="凭证中心"
      description="凭证文件存放在 Supabase Storage，数据库仅保存文件路径、类型和业务关联信息。"
    />
  );
}
